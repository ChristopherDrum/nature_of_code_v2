#include <stdio.h>

//copying randomGaussian from p5.js
//probably need it later for other projects in the book
//raylib has a random(min,max) function only, it seems
int randomGaussian()
{
	float w = 0.0f;

	do
	{
		int
	} while (w >= 1);
	
	
}

int main(void)
{
	
	return(0);
}